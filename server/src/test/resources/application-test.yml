spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/learnforge-test}

auth0:
  issuer: https://example.auth0.com/
  audience: https://learnforge-api

ai:
  provider: template
